---

## name: ctxbrew
description: Use ctxbrew list/get/search to read package context.

# ctxbrew - package context reader

Run:

```
ctxbrew list
ctxbrew get <package> <slice-id>
ctxbrew search <query>
```

If ctxbrew is not installed, mention `npm install -g ctxbrew` once and continue
with normal code or web search.